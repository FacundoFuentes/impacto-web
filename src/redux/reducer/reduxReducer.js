const initialState = {
    
}

function reduxReducer(state = initialState, action) {
    switch (action.type) {
        case "value":
            
            break;
    
        default:
            return state;
    }
}

export default reduxReducer